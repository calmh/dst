/*

Package miniudt implements a variant of "UDT: UDP-based Data Transfer
Protocol" as defined in draft-gg-udt-03.txt.

UDT is a way to get reliable stream connections (like TCP) on top of UDP.
MiniUDT implements a subset of the proper UDT protocol, which some changes.

*/
package miniudt
